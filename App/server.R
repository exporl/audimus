# Clear workspace
rm(list=ls(all=TRUE)) 

# Load packages
library(shiny)
library(shinyBS)

# --------------------------------- Begin Shiny Server ------------------------------
shinyServer(function(input, output) {
})


# --------------------------------- End Shiny Server --------------------------------